<% provide(:title, "Edit book") %>
<h1>Edit</h1>

<%=form_for(@book) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :author %>
	<%= f.text_field :author %>

	<%= f.label :publisher %>
	<%= f.text_field :publisher%>

	<%= f.submit "Edit" %>

<% end %>